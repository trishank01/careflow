# ExternalSecrets Operator (ESO) Resources
# Write your ClusterSecretStore and ExternalSecret manifests to pull:
#   - Database credentials from AWS SSM Parameter Store
#   - OpenAI API Key from AWS SSM Parameter Store
