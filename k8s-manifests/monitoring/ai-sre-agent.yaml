# AI SRE Agent Deployment and Service
# Write your Kubernetes Deployment, ServiceAccount, and ClusterRole/ClusterRoleBinding definitions for the AI SRE Agent.
# Grant read-only permissions for log and status queries on pods across namespaces.
