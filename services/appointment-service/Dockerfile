# Appointment Service Dockerfile
# Write a multi-stage Dockerfile to build and package this Java/Spring Boot application.
# Tip: Build inside a Maven container (stage 1) and copy the compiled jar file to a slim runtime JRE base image (stage 2).
