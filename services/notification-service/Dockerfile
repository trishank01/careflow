# Notification Service Dockerfile
# Write a multi-stage Dockerfile to build and package this Node.js application.
# Tip: Copy only the built code and package.json dependency references to keep dependencies clean.
