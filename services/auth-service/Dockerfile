# Auth Service Dockerfile
# Write a multi-stage Dockerfile to build and package this Python/FastAPI application.
# Tip: Ensure it runs as a non-root user and excludes development libraries to keep the image slim.
